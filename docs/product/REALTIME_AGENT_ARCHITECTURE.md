# Moved: Real-Time Agent Architecture

Canonical runtime-facing content now lives at `runtime/architecture/REALTIME_AGENT_ARCHITECTURE.md`.

This file is kept only as a compatibility pointer for older docs and validators.
